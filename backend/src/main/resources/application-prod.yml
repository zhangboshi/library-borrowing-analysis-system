spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:10}
  jpa:
    show-sql: false

logging:
  file:
    name: ${LOG_DIR:/var/log/library}/library-app.log
  level:
    root: INFO
